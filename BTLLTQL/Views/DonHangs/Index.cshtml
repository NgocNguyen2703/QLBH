@model IEnumerable<BTLLTQL.Models.DonHang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IDDichVu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IDNhanVien)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IDDichVu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IDNhanVien)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IĐonHang }) |
            @Html.ActionLink("Details", "Details", new { id=item.IĐonHang }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IĐonHang })
        </td>
    </tr>
}

</table>
