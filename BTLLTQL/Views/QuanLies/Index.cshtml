@model IEnumerable<BTLLTQL.Models.QuanLy>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IDNhanVien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IDBan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IDCa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.IDNhanVien)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.IDBan)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.IDCa)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.IDNhanVien }) |
        @Html.ActionLink("Details", "Details", new { id = item.IDNhanVien }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.IDNhanVien })
    </td>
</tr>
}

</table>
